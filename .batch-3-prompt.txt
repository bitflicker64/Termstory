EXECUTE ONLY. NO subagents. NO thinking.

You are at /Users/himanshuverma/personal/termstory.

CRITICAL WORKFLOW — Follow EXACTLY in order:

=== GREPTILE REVIEW PROTOCOL (FOLLOW THIS EXACTLY) ===
Greptile is a GitHub app installed on bitflicker64/Termstory. It EDITS its review comment IN-PLACE when re-scanned. The `gh pr view --json reviews` array shows STALE data. To get the real latest score:

STEP: Check Greptile score = get the LATEST Greptile comment body:
  LATEST_BODY=$(gh pr view <number> --repo bitflicker64/Termstory --json comments --jq '[.comments[] | select(.author.login == "greptile-apps")] | last | .body')
  SCORE=$(echo "$LATEST_BODY" | grep -oE '[0-9]/5' | head -1 | cut -d/ -f1)
  echo "Greptile score: $SCORE/5"

DECISION RULES (STRICT):
  - Score >= 4 → gh pr merge <number> --squash --delete-branch
  - Score < 4 → READ EVERY inline comment in the Greptile body, fix ALL issues in the code, then:
    git add -A && git commit -m "fix: address Greptile review feedback" && git push
    gh pr comment <number> --body "@greptileai review"
    echo "Waiting 120s for Greptile re-scan..." && sleep 120
    RE-CHECK score from the LATEST comment body (NOT reviews array)
    Repeat until score >= 4 or max 5 attempts
  - If Greptile never responds after 5 attempts: report "Greptile unreachable" and STOP. Do NOT merge.

=== BATCH 3 TASKS ===

STEP 1 — Branch off main:
  git checkout main && git pull && git checkout -b feat/batch-3-fts5-stress-context

STEP 2 — SQLite FTS5 Integration:
  - Read database.py to understand current schema and search implementation
  - Add FTS5 virtual tables for commands, sessions, and AI summaries
  - Update search queries to use FTS5 MATCH for ranked full-text search
  - Add migration logic for existing databases (create FTS tables if not exist)
  - Key files: database.py, formatter.py (search formatting), cli.py (search command)
  - Run tests: python3 -m pytest tests/ -x -v -k "search or database" 2>&1
  - Fix any failures

STEP 3 — Concurrency Stress Tests:
  - Read test_database.py to understand existing test patterns
  - Create tests/test_stress.py with:
    - Massive history simulator: synthesizes 10,000+ commands across 500+ sessions spanning 2 years
    - Concurrent reader/writer tests simulating TUI + CLI + AI summary access
    - Race condition detector: verify no deadlocks under heavy parallel load
    - 30-second timeout per test to catch hangs
  - Run: python3 -m pytest tests/test_stress.py -x -v 2>&1
  - Fix any failures

STEP 4 — Project-Specific AI Contexts:
  - Read ai.py to understand the current prompt system
  - Add project_context column to projects table in database.py
  - Add CLI command: termstory project context <name> --set "description"
  - Modify _send_llm_request or prompt builder to inject project context into AI prompts
  - Update formatter.py to show project context in project output
  - Run tests: python3 -m pytest tests/ -x -v 2>&1
  - Fix any failures

STEP 5 — Full test suite:
  source venv/bin/activate && python3 -m pytest tests/ -v 2>&1
  Note: some tests may have pre-existing failures unrelated to your changes. Check if failures exist on main first. Fix only failures caused by your changes + any pre-existing ones that block merge.

STEP 6 — Commit and push:
  git add -A && git commit -m "feat: FTS5 search, stress tests, project AI contexts" && git push origin feat/batch-3-fts5-stress-context

STEP 7 — Create PR:
  gh pr create --base main --head feat/batch-3-fts5-stress-context --title "Batch 3: FTS5 search + stress tests + project AI contexts" --body "Implements 3 roadmap items:\n1. SQLite FTS5 integration for ranked full-text search\n2. Concurrency stress tests with massive history simulation\n3. Project-specific AI context injection into LLM prompts" --fill

STEP 8 — Greptile review loop:
  Poll every 30s for Greptile to post its review. Use the COMMENT BODY method above.
  Score >= 4 → merge
  Score < 4 → fix ALL issues → commit+push → @greptileai review → wait 120s → re-check → repeat

STEP 9 — Done:
  git checkout main && git pull
  Print PR URL and final Greptile score.

RULES:
- NO subagents — use define_subagent and invoke_subagent are BANNED (they hang)
- Work directly on files using read/write/edit tools
- After each task, run its tests before moving to next
- If a task fails after 3 attempts, skip it and move to next, report at end
- User-facing code must never show raw tracebacks — catch exceptions and show friendly messages
- Log full errors to stderr for debugging
